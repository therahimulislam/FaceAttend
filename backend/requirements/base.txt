# =============================================================================
# FaceAttend — Base Requirements
# =============================================================================

# Django core
Django>=5.1,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.3,<5.0
django-environ>=0.11,<1.0
django-filter>=24.0,<25.0

# Database
psycopg2-binary>=2.9,<3.0

# ASGI / WebSocket
daphne>=4.1,<5.0
channels>=4.1,<5.0
channels-redis>=4.2,<5.0

# Cache / Task queue
redis>=5.0,<6.0
django-redis>=5.4,<6.0
celery>=5.4,<6.0
django-celery-beat>=2.6,<3.0
django-celery-results>=2.5,<3.0

# Image processing
Pillow>=10.4,<12.0
opencv-python-headless>=4.8,<6.0

# Face recognition (Phase 9+)
# InsightFace ArcFace model via ONNX — no dlib/cmake required
insightface>=1.0,<2.0
onnxruntime>=1.18,<2.0

# Static files (production)
whitenoise>=6.7,<7.0

# Utilities
python-decouple>=3.8
# Phase 15 — Reporting
openpyxl>=3.1,<4.0
reportlab>=4.0,<6.0
